# secrets — never commit
.env
.env.*
!.env.example

# the database holds character names
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
data/

# regenerable from design/*.dc.html via the design skill
design/raid-bingo-screens.html

node_modules/
dist/
*.log
.DS_Store

# fonts are fetched per-deployment, not committed (see fonts/README.md)
fonts/*.ttf
fonts/*.otf
fonts/OFL-*.txt

# guild squares name real people; keep them out of a public repo
presets.json

# working notes, not for the public repo
MEMORY.md
